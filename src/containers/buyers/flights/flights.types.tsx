export interface IFlightData {
  name: string;
  name1: string;
  phoneNumber: string;
  email: string;
  call: string;
  call1: string;
}

export interface IPaymentData {
  cardNumber: string;
  cardName: string;
  cvv: string;
  expiredDate: string;
}
